# Install when face_recognition/dlib cannot be built (e.g. Windows without Visual C++).
# Detection (SCRFD) and tests (matcher, embedding shape) work; enroll/find need face_recognition.
onnxruntime>=1.17.0
opencv-python>=4.8.0
numpy>=1.24.0
pytest>=7.0.0
