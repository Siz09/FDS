# Base deps (no face_recognition). Use on Windows to avoid dlib build.
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.30.0
structlog>=24.0.0
mediapipe>=0.10.0
opencv-python>=4.8.0
numpy>=1.24.0
pytest>=7.0.0
